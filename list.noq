list :: (A, B, ..) = (A + 1, B + 1, ..)
vec_add :: (A, ..) + (B, ..) = (A + B, ..)
vec_test :: (A + B, C + D, ..) = (A, C, ..) + (B, D, ..)
vec_test2 :: (A + B, ..) = (A, ..) + (B, A, ..)
intersperse :: ((A, ..), (B, ..)) = (A, B, ..)
// TODO: There's a bug where starting a shape
//     on line below a rule includes the shape in the rule.
//     Only occurs when loading from file since repl only accepts single line input
(1 + 2, 3 + 4)